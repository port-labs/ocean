type: clickup
description: ClickUp integration for Port Ocean
icon: Clickup
features:
  - type: exporter
    section: Project Management
    resources:
      - kind: issue
      - kind: project
      - kind: team
configurations:
  - name: clickupPersonalToken
    description: The ClickUp personal token used for authentication. To create a personal token, see the <a href="https://clickup.com/api/developer-portal/authentication#personal-token">ClickUp documentation</a>
    required: true
    type: string
    sensitive: true
  - name: clickupBaseUrl
    description: The Clickup Base URL. Defaults to https://api.clickup.com when not specified
    type: url
    required: false
    default: https://api.clickup.com
  - name: includeArchivedProjects
    description: This is to filter archived projects. If set to true, archived projects will be included in the export. If set to false, archived projects will be excluded from the export. Default is false.
    type: boolean
    required: false
    default: "false"
  - name: appHost
    required: false
    type: url
    description: "The host of the Port Ocean app. Used to set up the integration endpoint as the target for Webhooks created in Clickup"
