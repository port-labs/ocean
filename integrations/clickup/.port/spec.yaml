description: clickup integration for Port Ocean
icon: Clickup
features:
  - type: exporter
    section: Project management
    resources:
      - kind: team
      - kind: project
      - kind: issue
configurations:
  - name: clickupApiToken
    required: true
    type: string
    description: "Clickup API key used for authentication and it serves as the authorization header"
    sensitive: true
