spec:
  export:
    incremental: false
    interval: true  
    kinds: 
      topic:
        schema:
          topicName: 
            type: string
          partition:
            type: number
          replicationFactor:
            type: number
          config:
            type: object
            properties:
              key:
                type: string
              value:
                type: string
  actions:
    createTopic:
      schema:
        topicName: 
          type: string
        partition:
          type: number
        replicationFactor:
          type: number
        config:
          type: object
          properties:
            key:
              type: string
            value:
              type: string
    deleteTopic:
      schema:
        topicName: 
          type: string

    

