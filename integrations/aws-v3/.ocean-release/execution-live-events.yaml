bump: minor
changelog-type: feature
changelog: Added live events support for CodeBuild build runs (including RetryBuild), CodeDeploy deployments, and CodePipeline pipeline executions via CloudTrail start/stop events. Build run live-event fetches now raise not-found when batch_get_builds omits the build instead of upserting an empty entity.
