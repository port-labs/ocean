{
  "identifier": "sesEmailIdentity",
  "title": "SES Email Identity",
  "icon": "AWS",
  "schema": {
    "properties": {
      "identityType": {
        "type": "string",
        "title": "Identity Type",
        "description": "The type of email identity (EMAIL_ADDRESS or DOMAIN)"
      },
      "verifiedForSendingStatus": {
        "type": "boolean",
        "title": "Verified For Sending",
        "description": "Indicates whether the email identity is verified for sending"
      },
      "feedbackForwardingStatus": {
        "type": "boolean",
        "title": "Feedback Forwarding Status",
        "description": "Whether feedback forwarding is enabled for the identity"
      },
      "dkimStatus": {
        "type": "string",
        "title": "DKIM Status",
        "description": "The status of the DKIM configuration for the email identity"
      },
      "mailFromDomain": {
        "type": "string",
        "title": "Mail From Domain",
        "description": "The custom MAIL FROM domain associated with the identity"
      },
      "mailFromDomainStatus": {
        "type": "string",
        "title": "Mail From Domain Status",
        "description": "The status of the custom MAIL FROM domain"
      },
      "configurationSetName": {
        "type": "string",
        "title": "Configuration Set Name",
        "description": "The configuration set applied to this identity"
      },
      "tags": {
        "type": "array",
        "title": "Tags",
        "description": "Tags associated with the email identity",
        "items": {
          "type": "object"
        }
      },
      "region": {
        "type": "string",
        "title": "Region",
        "description": "AWS region where the email identity is configured"
      }
    },
    "required": []
  },
  "mirrorProperties": {},
  "calculationProperties": {},
  "aggregationProperties": {},
  "relations": {
    "account": {
      "title": "Account",
      "target": "awsAccount",
      "required": false,
      "many": false
    }
  }
}
