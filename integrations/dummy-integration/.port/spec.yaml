description: Dummy Integration integration for Port Ocean
icon: Cookiecutter  # Should be one of the available icons in Port
features:
  - type: exporter
    section: Under Development  # Should be one of the available sections in Port
    resources:
      - kind: dummy-integration-example-kind
      # - kind: <ResourceName2>
configurations:
  - name: mydummy-integrationToken
    # required: true
    type: string
    sensitive: true
  - name: someApplicationUrl
    type: url
