bump: patch
changelog-type: bugfix
changelog: Fix GitHub 403 tree-fetch handling to preserve entities during outages (PORT-18430). Raise GitHubTreeFetchError on 403 permission errors to signal reconciliation guard and prevent entity deletion during API outages.
