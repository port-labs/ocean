version: v0.1.0
type: argocd
description: Argocd integration for Port Ocean
icon: Argo
features:
  - type: exporter
    section: Kubernetes Stack
    resources:
      - kind: project
      - kind: application
      - kind: deployment
configurations:
  - name: token
    required: true
    type: string
    sensitive: true
  - name: serverUrl
    type: string
