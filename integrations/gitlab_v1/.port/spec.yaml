description: gitlab integration for Port Ocean
icon: Cookiecutter  # Should be one of the available icons in Port
features:
  - type: exporter
    section: Under Development  # Should be one of the available sections in Port
    resources:
      - kind: gitlab_v1-example-kind
      # - kind: <ResourceName2>
configurations:
  - name: mygitlab_v1Token
    # required: true
    type: string
    sensitive: true
  - name: someApplicationUrl
    type: url
