resources:
  - kind: component
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"component"'
          properties:
            name: .metadata.name
  - kind: template
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"template"'
  - kind: API
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"api"'
  - kind: group
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"group"'
  - kind: user
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"user"'
  - kind: resource
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"resource"'
  - kind: system
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"system"'
  - kind: domain
    selector:
      query: "true"
    port:
      entity:
        mappings:
          identifier: .metadata.name
          title: .metadata.title // .metadata.name
          blueprint: '"domain"'
