description: bitbucket-cloud integration for Port Ocean
icon: Bitbucket  # Should be one of the available icons in Port
features:
  - type: exporter
    section: Git Providers # Should be one of the available sections in Port
    resources:
      - kind: repository
      - kind: pullRequest
      - kind: component
      - kind: project
      # - kind: <ResourceName2>
configurations:
  # - name: mybitbucket-cloudToken
  #   required: true
  #   type: string
  #   sensitive: true
  - name: workspaceId
    type: string
    sensitive: true
    required: true
  - name: username
    type: string
    sensitive: true
    required: true
  - name: password
    type: string
    sensitive: true
    required: true
