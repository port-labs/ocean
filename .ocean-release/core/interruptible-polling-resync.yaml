bump: minor
changelog-type: feature
changelog: |
  Polling listener now cancels running resyncs when new requests arrive (config change, mapping update, resync button click).
  Resyncs run in background, so polling checks continue every 60s regardless of resync duration.
